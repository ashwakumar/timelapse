# Server only. Do not prefix this token with NEXT_PUBLIC_.
OPENTSLM_API_URL=
OPENTSLM_API_TOKEN=

# Browser-safe demo configuration.
NEXT_PUBLIC_DEMO_MODE=fixture
NEXT_PUBLIC_EVALUATION_URL=/evaluation/results.v1.json

# Optional strict input-only VitalDB export; switches telemetry and defaults inference to backend.
NEXT_PUBLIC_TELEMETRY_URL=
