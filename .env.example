# Copy to .env and fill in. .env is gitignored; this template is not.
#
#   cp .env.example .env
#   set -a; source .env; set +a
#
# Hugging Face access token, used to download the OpenTSLM SoftPrompt checkpoint
# and its gated base language model. Create one at
# https://huggingface.co/settings/tokens with read access, then request access to
# the base model at https://huggingface.co/meta-llama/Llama-3.2-1B
HF_TOKEN=
